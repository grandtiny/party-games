diff --git a/dist/actions/betting.js b/dist/actions/betting.js
--- a/dist/actions/betting.js
+++ b/dist/actions/betting.js
@@ -39,7 +39,7 @@ function handleFold(state, action) {
         timeBankActiveSeat: null,
         timestamp: action.timestamp,
     };
-    const playersWithLiveHands = currentState.players.filter((p) => p && p.status !== "FOLDED" /* PlayerStatus.FOLDED */);
+    const playersWithLiveHands = currentState.players.filter((p) => p && (p.status === "ACTIVE" /* PlayerStatus.ACTIVE */ || p.status === "ALL_IN" /* PlayerStatus.ALL_IN */));
     if (playersWithLiveHands.length === 1 && playersWithLiveHands[0]) {
         return awardPotToLastPlayer(currentState, playersWithLiveHands[0].seat);
     }
diff --git a/dist/actions/validation.js b/dist/actions/validation.js
--- a/dist/actions/validation.js
+++ b/dist/actions/validation.js
@@ -157,8 +157,8 @@ function validateDealAction(state) {
             throw new illegal_action_error_1.IllegalActionError(types_1.ErrorCodes.CANNOT_DEAL, "Cannot deal while hand is in progress", { street: state.street });
         }
     }
     const isTournament = !!state.config.blindStructure;
-    const activePlayers = state.players.filter((p) => p && p.stack > 0 && (isTournament || !p.isSittingOut));
+    const activePlayers = state.players.filter((p) => p && p.stack + p.pendingAddOn > 0 && (isTournament || !p.isSittingOut));
     if (activePlayers.length < 2) {
         throw new illegal_action_error_1.IllegalActionError(types_1.ErrorCodes.NOT_ENOUGH_PLAYERS, `Need at least 2 players to deal, found ${activePlayers.length}`, { playerCount: activePlayers.length });
     }
diff --git a/dist/rules/action-order.js b/dist/rules/action-order.js
--- a/dist/rules/action-order.js
+++ b/dist/rules/action-order.js
@@ -169,8 +169,8 @@ function isActionComplete(state) {
     if (activeCount === 0) {
         // Only return true if we're in a hand (not pre-deal)
         // Check: Are there all-in players with bets?
         const allInPlayers = state.players.filter((p) => p?.status === "ALL_IN" /* PlayerStatus.ALL_IN */);
-        return allInPlayers.length > 0 && state.currentBets.size > 0;
+        return allInPlayers.length > 1 && (state.currentBets.size > 0 || state.pots.length > 0);
     }
     return activeCount > 0 && activeCount === actedCount;
 }
