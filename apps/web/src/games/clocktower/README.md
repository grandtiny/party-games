# 血染钟楼前端边界

本目录拥有血染钟楼的入口页、房间页、游戏组件和主题变量。

- 平台大厅、设置页和通用顶部栏位于 `src/platform`，不要在血染主题改动中覆盖。
- 新增视觉样式必须以 `.app-shell--clocktower` 为根作用域，主题变量集中在 `theme.css`。
- 圆桌头像继续承担入座、夜间选人、提名和猎手目标选择，不把这些操作迁出圆桌。
- 不使用运行时外链字体；新增图片前确认公开仓库可使用的许可并压缩资源。
- `src/styles.css` 仍包含现有通用和功能样式。新增样式优先写入本目录，后续再按实际收益迁移旧规则。
